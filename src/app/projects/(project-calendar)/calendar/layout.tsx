import React from 'react';

/**
 * プロジェクトカレンダーレイアウト
 * @param children
 * @returns JSX
 */
export default function ProjectsCalendarLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    return <div className="">{children}</div>;
}
