import React from 'react';

/**
 * プロジェクト統計レイアウト
 * @param children
 * @returns JSX
 */
export default function ProjectsStatisticsLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    return <div className="">{children}</div>;
}
